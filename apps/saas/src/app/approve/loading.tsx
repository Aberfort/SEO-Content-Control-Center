export default function Loading() {
  return (
    <main className="auth-page">
      <section className="auth-panel approval-panel">
        <p className="empty-copy">Loading request…</p>
      </section>
    </main>
  );
}
